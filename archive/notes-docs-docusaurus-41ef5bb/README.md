## Archived Docusaurus Notes Site

This directory preserves the last Docusaurus-based `notes-docs` implementation before the project moved to Starlight.

- Source commit: `41ef5bb`
- Archived on: `2026-03-23`
- Archived subtree: `notes-docs/`

### What is inside

The snapshot includes:

- the Docusaurus app configuration
- the three docs-instance setup for economics, mathematics, and programming
- the original theme files and static assets

### Why it exists

The active notes site now lives in the top-level [`notes-docs/`](/Users/lizhicheng/Desktop/PersonalWebsite/notes-docs) directory and uses Starlight.

This archive is kept so the Docusaurus version can be referenced, revived, or selectively reused later without depending on Git history lookup.
