## Archived Starlight Notes Site

This directory preserves the Starlight-based `notes-docs` implementation that temporarily replaced the Docusaurus notes site.

- Archived on: `2026-03-23`
- Archived source directory: `notes-docs/`

### What is inside

The snapshot includes:

- the standalone Astro + Starlight app
- the three top-level docs sections for economics, mathematics, and programming
- the generated `dist/` output and the local install state that existed at the time of archiving

### Current active site

The active notes site now lives again in [`notes-docs/`](/Users/lizhicheng/Desktop/PersonalWebsite/notes-docs) and uses Docusaurus.

This archive is kept so the Starlight version can still be referenced, revived, or selectively reused later.
